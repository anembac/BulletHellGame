package BulletHell.Model;

public class Util {


    /*
    This is a placeholder for a method that calculates which points to put objects into in the grid

    Might be a good idea to move it into the Grid class later on if necessary, however it can stay here for now.
     */
    public static boolean[][] calculateSize(IGameObject obj){
        boolean[][] filledSpots = new boolean[][] {new boolean[] {false}};

        return filledSpots;
    }


}
